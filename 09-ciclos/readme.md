Explicamos el ciclo `for`, sus partes y como utilizarlo combinado con estructuras de control `if` y `arrays`.  
Todo está en el video de la clase. Los ejemplos los hicimos en la consola del navegador.